# 2026-02-15 Late Night Memory Flush (~22:15)

## Flush [22:15] — Infrastructure + DNS + MailerLite

### What Shipped (9pm-10pm)

**SEO & PWA Enhancement (sub-agent):**
- OG tags, Twitter cards, meta descriptions on frontend
- PWA manifest updated ("TitleRun — Dynasty Fantasy Football")
- `usePageTitle` hook on 7 key pages
- robots.txt created
- Commit `195f59a`, score 99/100

**CI/CD + Error Tracking + Analytics (sub-agent):**
- GitHub Actions CI for frontend (.github/workflows/ci.yml)
- Sentry-ready error tracking (`src/utils/errorTracking.js`) — opt-in via env var
- Analytics scaffolding (`src/utils/analytics.js`) — provider-agnostic
- 5 key events wired (trade_analysis, player_compared, onboarding, draft_companion, page views)
- All disabled by default (privacy-first)
- Score 97/100
- ⚠️ Backend CI committed locally but NOT pushed (OAuth scope issue)

**Cloudflare DNS Migration (Taylor did this live):**
- `titlerun.co` added to Cloudflare (free plan)
- DNS records configured:
  - `@` CNAME → `titlerun-landing.pages.dev`
  - `api` CNAME → `nkntlnmt.up.railway.app`
  - `app` CNAME → `titlerun-app.pages.dev`
  - `www` CNAME → `titlerun-landing.pages.dev`
  - MX + TXT records preserved (email forwarding)
- Nameservers changed in Namecheap: `aisha.ns.cloudflare.com` + `martin.ns.cloudflare.com`
- Custom domains added in Cloudflare Pages for both `titlerun-landing` and `titlerun-app`
- **Status: Waiting for DNS propagation** (usually minutes, up to 24h)

**MailerLite Setup (Taylor did this):**
- Account created: Taylor Hawes, jeffdanielsbymail@gmail.com
- Account ID: `2116834`
- Group name: TitleRun (free plan, 14-day trial then free forever tier)
- 1 subscriber already (Taylor testing)
- Form ID: `37189961`, Form endpoint: `179527595979703683`

**Landing Page Email Forms Wired:**
- All 3 forms (hero, mid-page, footer) connected to MailerLite
- First commit `4ac056a` had `target="_blank"` bug → showed raw JSON
- Fixed with AJAX submit + inline success message (commit `f87c5b0`)
- Success shows: "✅ You're in! Check your inbox."
- Uses `mode: no-cors` + FormData POST

### Key DNS Info (for future reference)
- **Cloudflare nameservers:** `aisha.ns.cloudflare.com`, `martin.ns.cloudflare.com`
- **Cloudflare Zone ID:** `c9fe3271361553b91d5015d53287fe43`
- **Cloudflare Account ID:** `b79e7190e0fb9e7b962089f7678051d0`
- **Namecheap account:** taylorh1jr

### Decisions
- Cloudflare DNS (free) over Namecheap BasicDNS — CNAME flattening for root domain
- AJAX form submission over native form POST — better UX (inline success)
- All tracking/analytics opt-in by default — privacy-first

### Taylor's Remaining Actions
1. ~~Cloudflare DNS~~ ✅ DONE
2. ~~MailerLite signup~~ ✅ DONE  
3. ~~MailerLite embed code~~ ✅ DONE (wired into landing page, hidden iframe approach)
4. ~~titlerun.co live~~ ✅ DONE (DNS propagated, SSL active)
5. Railway env vars: `COLD_START_THRESHOLD=50`, `ANTHROPIC_API_KEY`
6. Railway: Add Redis service (optional but recommended)
7. Verify `app.titlerun.co` updated after Cloudflare build fix deploys

### Cloudflare Pages Build Fix (~22:30)
- **Root cause: `CI=TRUE` env var** — Treats all lint warnings as build errors
- **47 warnings across 8 files** — Unused imports, missing hook deps
- All fixed in commit `25fe1a2`
- **Branch mismatch found:** Production deploys from `main`, all code was on `master` (27 commits ahead)
- Fixed: Force-pushed `main` to match `master` — both now at `25fe1a2`
- **Production should now deploy successfully** — All tonight's work goes live

### MailerLite Integration Debugging (~22:07-22:30)
- **4 attempts to wire email capture:**
  1. `no-cors` fetch — silently drops request body ❌
  2. JSONP script injection — endpoint returns success for any request but may not actually add subscriber ❌
  3. MailerLite webforms.min.js — conflicted with custom handlers ❌
  4. **Hidden iframe form submission** — ✅ WORKS (commit `05dea9e`)
- **Key discovery:** Subscribers WERE being added but showing as unconfirmed (no date in "Subscribed" column)
- Switching MailerLite filter from "Active" to "All" revealed 6 subscribers total
- Hidden iframe = bulletproof: standard HTML form POST through invisible iframe, no CORS/JS issues

### Blocked
- Backend CI push — needs Git credential with `workflow` OAuth scope
- AI Chat in production — needs `ANTHROPIC_API_KEY` on Railway

### Critical Patterns Learned
- **🔴 `CI=TRUE` + CRA = build failures** — Every unused import or missing hook dep kills the build. Must lint before pushing.
- **🔴 Cloudflare Pages production branch = `main`** — All code must be on `main` to go live. `master` = preview only.
- **🔴 MailerLite JSONP endpoint returns `success:true` for ANY request** — Even empty ones. Not a reliable indicator of actual subscription.
- **🔴 Hidden iframe = only reliable cross-origin form submission** — `no-cors` fetch, JSONP, and MailerLite's own JS all had issues.
- **🔴 Parallel sub-agent builds create lint debt** — 47 warnings accumulated from 8+ agents building simultaneously.

### Portfolio State (end of day)
- **Feb 14-15 combined:** Probably the two biggest build days in TitleRun history
- **Feb 15 total commits:** ~50+ across 3 repos (frontend, backend, landing)
- **Infrastructure now:** Cloudflare DNS + Pages (frontend+landing), Railway (backend), MailerLite (email), GitHub Actions (CI)
- **Feature completeness:** ~90%+ across all pages
- **Landing page:** LIVE at titlerun.co with working email capture
- **App:** Pending Cloudflare build fix deploy (lint warnings fixed, branches synced)
- **Next phase:** Beta users, QA, performance tuning → March deadline (trade fairness + redraft)
- **Grind + Edge:** Still paused per Taylor directive
